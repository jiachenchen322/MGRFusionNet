# Minimal dependencies for the self-contained simulation study.
# The real-data workflow needs additional packages; see requirements.txt.

numpy>=1.26
scikit-learn>=1.3
torch>=2.1
torch-geometric>=2.4
