# Full dependencies for the real-data workflow.
# For the simulation study alone, use requirements-sim.txt instead.
#
# torch-scatter and torch-sparse must match your torch build. If pip cannot find
# a wheel, see the PyG installation guide:
# https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

numpy>=1.26
scipy>=1.11
pandas>=2.1
h5py>=3.10
matplotlib>=3.8
scikit-learn>=1.3
tqdm>=4.66
captum>=0.7
torch>=2.1
torch-geometric>=2.4
torch-scatter
torch-sparse
